#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
vector<int> v, dp;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    v.resize(n+1);
    dp.resize(n+1);
    for (int i=1; i<=n; i++) cin >> v[n];

}
