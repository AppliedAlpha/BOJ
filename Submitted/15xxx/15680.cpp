#include <iostream>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);
    bool i;
    cin >> i;
    cout << (i ? "Leading the Way to the Future" : "YONSEI");
    return 0;
}
